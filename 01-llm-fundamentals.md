# 01 - LLM 基础概念

> **难度：** ⭐⭐ | **定位：** 入门必读，建立对大语言模型的基本认知
>
> **前端类比：** 如果说前端框架（React/Vue）是 UI 渲染引擎，那 LLM 就是"文本生成引擎"——你给它输入（prompt），它给你输出（completion），和组件接收 props 返回 JSX 是一个思路。

## 本章知识树

```
LLM 基础
├── 1.1 什么是 LLM
├── 1.2 Token 与 Tokenizer
├── 1.3 生成参数（Temperature / Top-P / Top-K）
├── 1.4 上下文窗口（Context Window）
├── 1.5 LLM 的工作原理：Next Token Prediction
├── 1.6 主流模型对比（GPT / Claude / Gemini / 开源）
├── 1.7 LLM 的六大局限性
├── 1.8 LLM API 调用基础
└── 1.9 灾难性遗忘（Catastrophic Forgetting）
```

---

## 1.1 什么是 LLM

### Q: 什么是大语言模型（LLM）？它和传统 NLP 模型有什么区别？

**LLM（Large Language Model）= 参数量巨大的语言模型，通过海量文本训练，具备通用的语言理解和生成能力。**

核心区别：

| 维度 | 传统 NLP 模型 | LLM |
|------|---------------|-----|
| 参数量 | 数百万~数亿 | 数十亿~数万亿 |
| 训练数据 | 特定任务数据集 | 互联网规模文本 |
| 能力范围 | 单一任务（情感分析/翻译） | 通用任务（问答/写作/推理/编程） |
| 使用方式 | 需要针对每个任务微调 | Prompt 即可完成多种任务（Zero-shot） |
| 涌现能力 | 无 | 有（推理、CoT、In-context Learning） |

**前端类比：** 传统 NLP 就像写原生 JS 操作 DOM——每个交互都要手写逻辑。LLM 就像 React——你描述"想要什么"（声明式），框架帮你搞定。

**关键术语：**
- **预训练（Pre-training）：** 在海量文本上学习语言规律，类似前端的 `npm install`——装好基础能力
- **微调（Fine-tuning）：** 在特定数据上继续训练，类似对组件做 `customization`
- **涌现能力（Emergent Ability）：** 模型规模超过阈值后突然出现的能力（如推理、代码生成），小模型做不到

**面试话术：**
> "LLM 的核心突破是 Scaling Law——参数量和数据量到一定规模后，模型会'涌现'出训练时没有显式教过的能力，比如多步推理和代码生成。这打破了传统 NLP '一个任务训一个模型' 的范式。"

---

## 1.2 Token 与 Tokenizer

### Q: 什么是 Token？为什么 LLM 不是按"字"或"词"处理文本的？

**Token 是 LLM 处理文本的最小单位，介于字符和单词之间。**

```
英文示例：
"Hello world" → ["Hello", " world"] → 2 tokens
"unhappiness" → ["un", "happiness"] → 2 tokens（拆成词根）

中文示例：
"你好世界" → ["你好", "世界"] → 2-4 tokens（取决于 tokenizer）
"人工智能" → ["人工", "智能"] → 2 tokens
```

**换算关系：**

| 语言 | 近似关系 |
|------|----------|
| 英文 | 1 token ≈ 4 字符 ≈ 0.75 个单词 |
| 中文 | 1 个汉字 ≈ 1-2 个 tokens |

**为什么用 Token 而不是字/词？**

1. **子词切分（Subword Tokenization）** 兼顾了词汇表大小和覆盖度：
   - 按字符切：词汇表小但序列太长
   - 按单词切：词汇表太大且无法处理新词
   - 按子词切：平衡，且能处理未见过的词（拆成已知的子词组合）

2. **主流算法：** BPE（Byte Pair Encoding）——GPT 系列使用，从字符开始，反复合并最高频的相邻 token 对

**Token 对开发的影响：**
- **计费：** API 按 input tokens + output tokens 计费
- **上下文限制：** 模型能处理的总 token 数有上限
- **生成长度：** `max_tokens` 限制输出长度
- **成本估算：** 1000 tokens ≈ 750 英文单词 ≈ 500 中文字

**前端类比：** Token 就像 Virtual DOM 的节点——React 不直接操作 DOM 元素，LLM 不直接处理字符，都是用一个更适合计算的中间表示。

---

## 1.3 生成参数

### Q: Temperature、Top-P、Top-K 分别是什么？怎么调？

这三个参数控制 LLM 生成文本时的"随机性"——决定模型从候选词中怎么选下一个 token。

**Temperature（温度）：** 控制概率分布的"锐利程度"

```
假设下一个 token 的候选概率：
  "好" = 0.5, "棒" = 0.3, "妙" = 0.15, "差" = 0.05

Temperature = 0（贪心）：
  → 永远选"好"（概率最高的）

Temperature = 0.3（保守）：
  → 大概率选"好"或"棒"，几乎不选"差"

Temperature = 1.0（标准）：
  → 按原始概率采样

Temperature = 1.5（激进）：
  → 概率被拉平，"差"也有机会被选中
```

**Top-P（Nucleus Sampling，核采样）：** 只从累积概率 ≥ P 的最小候选集中采样

```
Top-P = 0.9：
  "好"(0.5) + "棒"(0.3) + "妙"(0.15) = 0.95 > 0.9
  → 只从 ["好", "棒", "妙"] 中选，排除"差"
```

**Top-K：** 只从概率最高的 K 个词中采样

```
Top-K = 2：
  → 只从 ["好", "棒"] 中选
```

**调参最佳实践：**

| 场景 | Temperature | Top-P | 说明 |
|------|-------------|-------|------|
| RAG / 知识问答 | 0 ~ 0.2 | 0.9 | 要准确，不要创意 |
| 代码生成 | 0.2 ~ 0.3 | 0.95 | 允许少量变化 |
| 通用对话 | 0.5 ~ 0.7 | 0.9 | 平衡准确与自然 |
| 创意写作 | 0.8 ~ 1.0 | 0.95 | 要多样性 |

**注意：** Temperature 和 Top-P 通常只调一个，不建议同时设置极端值。

---

## 1.4 上下文窗口

### Q: 什么是上下文窗口（Context Window）？它对 AI 应用开发有什么影响？

**Context Window = LLM 一次能"看到"的最大 token 数量，包括输入 + 输出。**

```
以 128K context window 为例：

┌──────────────── 128K tokens ─────────────────┐
│  System Prompt  │  用户输入  │  检索内容  │ 输出 │
│   (~500)        │  (~200)   │ (~5000)   │(~2000)│
└─────────────────────────────────────────────────┘
可用空间 = 128K - 已用空间
```

**主流模型的 Context Window（2026）：**

| 模型 | Context Window | 说明 |
|------|---------------|------|
| GPT-4o | 128K | OpenAI 主力 |
| Claude 4 Sonnet/Opus | 200K / 1M | Anthropic，长文本强 |
| Gemini 2.0 | 1M ~ 2M | Google，超长上下文 |
| DeepSeek-V3 | 128K | 开源，性价比高 |
| Qwen3 | 128K | 阿里，中文强 |

**对开发的影响：**

1. **RAG 设计：** Context Window 小 → 需要精确检索少量 chunks；窗口大 → 可以塞更多上下文但成本高
2. **成本控制：** 越长的输入 = 越高的 API 费用
3. **Lost in the Middle：** 即使窗口很大，模型对中间部分的关注度也会下降
4. **多轮对话：** 历史消息会占用 context，需要做对话摘要或截断

**前端类比：** Context Window 就像浏览器的内存限制——页面太大会卡顿。LLM 的 context 太长，质量也会下降，需要"虚拟滚动"（只加载可见内容）的思路来优化。

---

## 1.5 LLM 的工作原理

### Q: LLM 是怎么生成文本的？什么是 Next Token Prediction？

**LLM 的核心原理极其简单：给定前面的文字，预测下一个最可能的 token。**

```
输入："今天天气"
模型内部：P("真" | "今天天气") = 0.4
          P("不" | "今天天气") = 0.3
          P("很" | "今天天气") = 0.2
          ...
→ 选择 "真"

输入变为："今天天气真"
模型内部：P("好" | "今天天气真") = 0.7
          P("热" | "今天天气真") = 0.2
          ...
→ 选择 "好"

循环直到生成结束标记（<EOS>）或达到 max_tokens
```

**这个简单原理为什么能产生"智能"？**

1. **Scaling Law：** 参数量足够大 + 训练数据足够多 → 模型学到了语言的深层结构和世界知识
2. **In-context Learning：** 模型能从 prompt 中的示例"临时学习"，不需要更新参数
3. **涌现能力：** 当模型达到一定规模（约 100B+ 参数），突然出现推理、代码生成等训练时没有显式教过的能力

**自回归 vs 非自回归：**

| 方式 | 代表 | 特点 |
|------|------|------|
| 自回归（Autoregressive） | GPT 系列 | 逐 token 生成，质量高但慢 |
| 非自回归 | 部分翻译模型 | 并行生成，快但质量差 |
| Encoder-Decoder | T5、BART | 先编码再解码 |

当前主流 LLM 全部使用 **Decoder-Only 自回归架构**（GPT、Claude、Gemini、LLaMA 都是）。

---

## 1.6 主流模型对比

### Q: 2026 年主流 LLM 有哪些？各有什么特点？如何选型？

**闭源 API 模型：**

| 模型 | 厂商 | 核心优势 | 适用场景 |
|------|------|----------|----------|
| GPT-4o | OpenAI | 综合能力强，生态成熟 | 通用应用 |
| Claude 4 Opus | Anthropic | 长上下文（1M），代码/推理强 | 复杂推理、长文档 |
| Claude 4 Sonnet | Anthropic | 性价比高，速度快 | 日常开发首选 |
| Gemini 2.0 | Google | 多模态原生，超长上下文 | 多模态应用 |
| 豆包 / Doubao | 字节跳动 | 中文优化，价格便宜 | 国内应用 |
| 通义千问 / Qwen | 阿里 | 中文强，开源版本可用 | 中文场景 |

**开源模型：**

| 模型 | 参数量 | 核心优势 |
|------|--------|----------|
| LLaMA 4 | 8B/70B/405B | Meta 出品，社区活跃 |
| DeepSeek-V3/R1 | 671B（MoE） | 推理能力极强，性价比高 |
| Qwen3 | 0.6B ~ 235B | 中文最佳开源 |
| Mistral Large | 123B | 欧洲出品，多语言 |

**选型决策树：**

```
需要什么能力？
├── 通用问答/对话 → GPT-4o / Claude Sonnet（性价比）
├── 复杂推理/编程 → Claude Opus / DeepSeek-R1
├── 长文档处理 → Claude（200K-1M）/ Gemini（1M+）
├── 中文场景 → Qwen3 / 豆包
├── 数据隐私/私有部署 → DeepSeek / Qwen / LLaMA（开源）
└── 多模态（图+文） → GPT-4o / Gemini 2.0
```

---

## 1.7 LLM 的局限性

### Q: LLM 有哪些主要的局限性？开发 AI 应用时要注意什么？

**六大局限性：**

| 局限 | 说明 | 应对方案 |
|------|------|----------|
| **幻觉（Hallucination）** | 模型会自信地编造不存在的事实 | RAG 提供检索依据；低 Temperature；要求引用来源 |
| **知识截止（Knowledge Cutoff）** | 训练数据有截止日期，不知道最新信息 | RAG 接入实时知识库；联网搜索 |
| **数学/逻辑弱** | 复杂计算和逻辑推理容易出错 | Chain of Thought；Tool Use（调用计算器） |
| **上下文遗忘** | 长对话/长文本中容易忽略中间信息 | 上下文压缩；关键信息前置；分段处理 |
| **无法真正"理解"** | 基于统计模式匹配，不是真正理解 | 明确 prompt 意图；多轮确认；人工审核 |
| **安全风险** | 可能被 Prompt 注入攻击，生成有害内容 | 输入过滤；输出审核；System Prompt 防护 |

**前端类比：** 就像你不会 100% 信任用户输入一样（XSS 防护），AI 应用也不能 100% 信任 LLM 的输出——需要验证、过滤、兜底。

**面试话术：**
> "LLM 最大的问题是幻觉——它会'一本正经地胡说八道'。作为 AI 应用开发者，我的核心工作就是用工程手段（RAG、验证层、Safety Guard）来弥补模型的不足，让 AI 应用从'能用'变成'可靠'。"

---

## 1.8 LLM API 调用基础

### Q: 如何调用 LLM API？核心参数有哪些？

**标准 API 调用结构（以 OpenAI 格式为例，Claude/Gemini 类似）：**

```python
from openai import OpenAI

client = OpenAI(api_key="sk-xxx")

response = client.chat.completions.create(
    model="gpt-4o",                    # 模型选择
    messages=[                         # 对话历史
        {"role": "system", "content": "你是一个前端技术专家"},
        {"role": "user", "content": "解释一下虚拟DOM"},
    ],
    temperature=0.3,                   # 随机性（0-2）
    max_tokens=1000,                   # 最大输出长度
    top_p=0.9,                         # 核采样
    stream=True,                       # 是否流式输出
)

# 流式读取
for chunk in response:
    content = chunk.choices[0].delta.content
    if content:
        print(content, end="")
```

**三种 Role 的含义：**

| Role | 作用 | 类比 |
|------|------|------|
| `system` | 设定 AI 的角色和行为规则 | 组件的 `defaultProps` |
| `user` | 用户的输入 | 组件的 `props` |
| `assistant` | AI 的回复 | 组件的 `render` 输出 |

**关键参数速查：**

| 参数 | 作用 | 建议值 |
|------|------|--------|
| `model` | 选择模型 | 按需选择 |
| `temperature` | 随机性 | 0~0.3（精确场景）|
| `max_tokens` | 输出上限 | 按需设置 |
| `top_p` | 核采样 | 0.9（通常不动）|
| `stream` | 流式输出 | 生产环境建议 `true` |
| `stop` | 停止词 | 特定格式时有用 |

**前端类比：** 调用 LLM API 就像调用一个特殊的后端接口——请求体是 `messages`，响应体是生成的文本。`stream: true` 就是 Server-Sent Events（SSE），前端工程师对此应该很熟悉。

**面试话术：**
> "LLM API 本质上就是一个 HTTP 接口。作为前端转 AI 的开发者，我对 SSE 流式传输、请求重试、错误处理这些都很熟悉，这些前端工程化经验可以直接迁移。"

---

## 1.9 灾难性遗忘

### Q: 什么是灾难性遗忘（Catastrophic Forgetting）？如何解决？

**灾难性遗忘 = 模型在学习新任务时，把旧任务学到的知识"忘掉"了。**

```
场景：你微调一个通用 LLM 做医疗问答
  微调前：能写代码 ✅、能翻译 ✅、能做数学题 ✅
  微调后：医疗问答很棒 ✅，但写代码变差了 ❌，翻译也不行了 ❌

→ 新知识"覆盖"了旧知识的权重，这就是灾难性遗忘
```

**为什么会发生？** LLM 的知识存储在参数（权重）中。微调时更新这些权重以适应新任务，但这会改变与旧任务相关的权重，导致旧能力退化。

**五种解决方案：**

| 方案 | 原理 | 效果 | 成本 |
|------|------|------|------|
| **LoRA/QLoRA** | 只训练低秩增量矩阵，冻结原始权重 | 最常用，基本不影响旧能力 | 低 |
| **EWC（弹性权重整合）** | 约束"重要"权重的变化幅度 | 好，但需要计算 Fisher 信息矩阵 | 中 |
| **数据混合** | 微调数据中混入通用数据（比例 1:5~1:10） | 简单有效 | 中 |
| **渐进式学习** | 逐步加入新任务，保留旧任务的 replay buffer | 持续学习场景 | 高 |
| **模型合并（Model Merging）** | 微调后与原始模型做权重平均 | 2024+ 新方法，简单有效 | 低 |

**LoRA 如何避免遗忘？**

```
原始模型权重 W（冻结不变）
  ↓
微调时只训练 ΔW = A × B（低秩矩阵）
  ↓
推理时：W_final = W + ΔW
  ↓
原始知识保留在 W 中，新知识存储在 ΔW 中
→ 旧能力不受影响！
```

**前端类比：** 灾难性遗忘就像用 `Object.assign(config, newConfig)` 直接覆盖了配置——新配置把旧配置的关键字段给覆盖掉了。LoRA 的做法像是 `{...config, ...override}`，保留原始配置，只叠加增量。

**面试话术：**
> "灾难性遗忘是微调的核心挑战。我的解决方案是 LoRA——它冻结原始权重，只训练一个低秩增量矩阵（通常只有原始参数的 0.1%-1%），这样新知识和旧知识互不干扰。如果还需要额外保障，可以在微调数据中混入 10-20% 的通用指令数据。"

---

[下一章：02 - Transformer 架构 →](./02-transformer.md)
