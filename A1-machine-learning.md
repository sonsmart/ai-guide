# A1: 机器学习基础 — AI 的前世

> **定位**：了解级别 | 前端转 AI 需要知道的 ML 常识
> **目标**：面试中能说清"ML 是什么"、经典算法直觉、和 LLM 的关系

---

## 知识树

```
机器学习基础（了解级别）
├── A1.1 什么是机器学习（监督/无监督/强化学习）
├── A1.2 经典算法速览
│   └── 线性回归 / 逻辑回归 / 决策树 / SVM / K-means
├── A1.3 核心概念
│   └── 过拟合/欠拟合、训练集/验证集/测试集、正则化
├── A1.4 特征工程
│   └── 为什么 LLM 时代特征工程变少了（Embedding 替代）
├── A1.5 评估指标
│   └── Accuracy / Precision / Recall / F1 / AUC
└── A1.6 ML vs DL vs LLM：三代 AI 的区别
```

---

## A1.1 什么是机器学习

### Q: 机器学习的三种主要范式是什么？

| 范式 | 核心思路 | 数据要求 | 典型例子 |
|------|---------|---------|---------|
| **监督学习 Supervised** | 给标注数据，学输入→输出映射 | 需要标签 (label) | 垃圾邮件分类、房价预测 |
| **无监督学习 Unsupervised** | 没有标签，发现数据内在结构 | 不需要标签 | 用户聚类、降维可视化 |
| **强化学习 Reinforcement** | Agent 通过奖励信号学习策略 | 环境反馈 (reward) | 游戏 AI、**RLHF**（LLM 对齐） |

### Q: 前端类比怎么理解这三种？

- **监督学习** ≈ 有 test case 的 TDD — 输入输出都定义好，让模型去"写实现"
- **无监督学习** ≈ 代码聚类/分组 — 没人告诉你怎么分，自动找 pattern
- **强化学习** ≈ A/B Testing 自动化 — 根据用户反馈（reward）不断调整策略

### Q: RLHF 和强化学习是什么关系？

RLHF（Reinforcement Learning from Human Feedback）是强化学习在 LLM 对齐中的应用。人类标注偏好数据 → 训练 reward model → 用 PPO 等 RL 算法优化 LLM 输出。这是 ChatGPT 能"听话"的关键。

---

## A1.2 经典算法速览

### Q: 经典 ML 算法各自做什么？什么时候用？

| 算法 | 一句话直觉 | 适用场景 |
|------|-----------|---------|
| **线性回归** | 画一条直线拟合数据点 | 连续值预测（房价、温度） |
| **逻辑回归** | 线性回归 + sigmoid，输出概率 | 二分类（是/否判断） |
| **决策树** | 一连串 if-else 规则，像流程图 | 可解释性强的分类/回归 |
| **SVM** | 找到最大间隔的分界线/超平面 | 小数据集高维分类 |
| **K-means** | 把数据分成 K 个簇，各簇中心最近 | 用户分群、数据聚类 |

### Q: 为什么面试还问这些"老"算法？

1. 考察基础功底 — 你是否理解 ML 的核心思想
2. 很多业务场景不需要 LLM — 推荐系统、风控、表格数据仍用经典 ML
3. 理解进化脉络 — 知道 LLM 解决了什么老问题

> **面试话术**：
> "经典 ML 算法在结构化数据（表格）上仍然很强。LLM 主要革新了非结构化数据（文本、图像）的处理方式。选型要看数据类型和业务需求。"

---

## A1.3 核心概念

### Q: 什么是过拟合和欠拟合？

| 概念 | 表现 | 前端类比 |
|------|------|---------|
| **过拟合 Overfitting** | 训练集表现好，测试集差 | hardcoding test case 而不是写通用逻辑 |
| **欠拟合 Underfitting** | 训练集和测试集都差 | 用 `if-else` 解决需要状态机的问题 — 模型太简单 |

### Q: 训练集/验证集/测试集怎么分？

```
全部数据
├── 训练集 Train (70-80%)  → 模型学习
├── 验证集 Val (10-15%)    → 调超参数、选模型
└── 测试集 Test (10-15%)   → 最终评估，只用一次
```

- **验证集** ≈ staging 环境 — 上线前测试
- **测试集** ≈ production 真实用户 — 不能反复调

### Q: 正则化是什么？

正则化 = 给损失函数加惩罚项，限制模型复杂度，防止过拟合。

- **L1 正则化**：倾向让参数变为 0（稀疏化，做特征选择）
- **L2 正则化**：让参数尽量小但不为 0（平滑化）

> 前端类比：正则化 ≈ ESLint 规则 — 限制代码的"自由度"以提高质量。

---

## A1.4 特征工程

### Q: 什么是特征工程？为什么 LLM 时代变少了？

| 时代 | 特征处理方式 | 例子 |
|------|------------|------|
| **传统 ML** | 手动设计特征（人工经验） | 从文本提取词频、TF-IDF、n-gram |
| **深度学习** | 模型自动学习特征（CNN/RNN） | 图像 → 自动提取边缘、纹理 |
| **LLM 时代** | Embedding 就是特征 | 文本 → `text-embedding-3-small` → 1536 维向量 |

### Q: 为什么说 Embedding 替代了特征工程？

传统 ML 需要人工把"猫的图片"变成"有胡须=1, 有翅膀=0, 体型=小"这样的特征向量。

LLM 时代直接把文本/图像输入模型，模型内部的 Embedding 层自动将输入映射到高维向量空间。**特征提取从"手动"变成"自动"**。

> **面试话术**：
> "传统 ML 是 80% 时间做特征工程，20% 调模型。LLM 时代反过来了 — Embedding 自动完成特征提取，工程师更多精力放在 prompt 设计和数据质量上。"

---

## A1.5 评估指标

### Q: Precision、Recall、F1 怎么理解？

以"垃圾邮件检测"为例：

| 指标 | 含义 | 公式 | 通俗理解 |
|------|------|------|---------|
| **Accuracy** | 整体正确率 | 正确数 / 总数 | 数据不均衡时会误导 |
| **Precision** | 查准率 | TP / (TP+FP) | 预测为垃圾的，真的是垃圾的比例 |
| **Recall** | 查全率 | TP / (TP+FN) | 所有真垃圾中，被找到的比例 |
| **F1** | P 和 R 的调和平均 | 2PR/(P+R) | 兼顾 P 和 R 的综合指标 |
| **AUC** | ROC 曲线下面积 | — | 模型区分正负样本的总体能力 |

### Q: Precision 和 Recall 的 tradeoff？

- **高 Precision 优先**：宁可漏掉，不能误报 — 如搜索推荐（推错比不推更差）
- **高 Recall 优先**：宁可误报，不能漏掉 — 如疾病筛查（漏诊代价极大）

### Q: 这些指标在 RAG 中还有用吗？

有用。RAG 评估中：
- **Recall** → 检索到的相关文档占所有相关文档的比例
- **Precision** → 检索结果中真正相关的比例
- 还衍生出 **Context Relevance**、**Faithfulness** 等 RAG 专用指标

---

## A1.6 ML vs DL vs LLM：三代 AI 的区别

### Q: ML、DL、LLM 三代 AI 有什么本质区别？

| 维度 | 传统 ML | 深度学习 DL | LLM |
|------|--------|-----------|-----|
| **时间** | 1990s-2012 | 2012-2020 | 2020-now |
| **代表** | SVM, 随机森林 | CNN, LSTM | GPT, BERT |
| **特征处理** | 手动特征工程 | 自动学习特征 | Embedding + Attention |
| **数据需求** | 少量结构化数据 | 大量标注数据 | 海量无标注文本 |
| **使用方式** | 训练 → 推理 | 训练 → 推理 | 预训练 → Prompt / Fine-tune |
| **泛化能力** | 单任务 | 单任务/多任务 | 通用 + 涌现能力 |
| **工程师角色** | 特征工程师 | 模型架构师 | Prompt / 应用工程师 |

### Q: 为什么说 LLM 是范式转移？

```
传统 ML:  数据 → 特征工程 → 模型训练 → 单一任务
深度学习:  数据 → 端到端训练 → 单一/少量任务
LLM:     海量数据 → 预训练 → Prompt/Fine-tune → 几乎任何 NLP 任务
```

关键区别：
1. **从专才到通才** — 一个 LLM 替代几十个专用模型
2. **从训练到提示** — 不需要重新训练，prompt 就能完成新任务
3. **涌现能力** — 规模足够大后出现训练目标之外的能力（推理、编码）

> **面试话术**：
> "ML 三代演进的核心趋势是：人工干预越来越少，模型能力越来越通用。传统 ML 靠人工特征，DL 靠自动特征学习，LLM 靠海量预训练 + prompt 直接完成任务。对前端工程师来说，LLM 的意义在于 — 我们不再需要成为 ML 专家也能构建 AI 应用。"

---

## 本章小结

| 知识点 | 面试一句话 |
|--------|----------|
| 三种学习范式 | 监督（有标签）、无监督（找结构）、强化（奖励驱动，RLHF） |
| 经典算法 | 结构化数据仍用经典 ML，LLM 主要革新非结构化数据 |
| 过拟合 | 模型记住训练数据而非学会泛化，正则化是解法 |
| 特征工程 | 传统手动 → DL 自动 → LLM 用 Embedding 替代 |
| 评估指标 | P/R/F1 在 RAG 评估中仍然重要 |
| ML vs DL vs LLM | 三代趋势：人工干预减少，通用能力增强 |

---

> **导航**
> ← 上一章：目录 | [A2: 深度学习基础](./A2-deep-learning.md) →
